
var window = makeWindow("I am a window", "autoResize")

window.show(true)
window.

var {const, global, obj|} test(a)(e)what = {
    hei = "wow",
    kva = "kva"
    hi = a + e
}

console.log(|test(a)(e)what.hi|)



